:host {
    text-align: center;
    
    ul {
        list-style-type: none;
        margin: 0;
        padding: 0;
        min-width: 50%;    
        text-align: center;
        margin: 0 auto;
    }

    li {
        padding: 7px;
        // color: #f44336;
    }

    li:last-child {
        border: none;
    }
}
